<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.DiscusscourseDao">

    <resultMap type="com.entity.DiscusscourseEntity" id="discusscourseMap">
        <result property="id" column="id"/>
        <result property="refid" column="refid"/>
        <result property="userid" column="userid"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarurl" column="avatarurl"/>
        <result property="content" column="content"/>
        <result property="reply" column="reply"/>
        <result property="likenum" column="likenum"/>
        <result property="stepnum" column="stepnum"/>
        <result property="addtime" column="addtime"/>
    </resultMap>

    <select id="selectListVO" resultType="com.entity.vo.DiscusscourseVO" >
        SELECT * FROM discusscourse discusscourse ${ew.customSqlSegment}
    </select>

    <select id="selectVO" resultType="com.entity.vo.DiscusscourseVO" >
        SELECT discusscourse.* FROM discusscourse discusscourse ${ew.customSqlSegment}
    </select>

    <select id="selectListView" resultType="com.entity.view.DiscusscourseView" >
        SELECT discusscourse.* FROM discusscourse discusscourse ${ew.customSqlSegment}
    </select>

    <select id="selectView" resultType="com.entity.view.DiscusscourseView" >
        SELECT * FROM discusscourse discusscourse ${ew.customSqlSegment}
    </select>


</mapper>
